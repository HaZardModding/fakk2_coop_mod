#include "cg_ctf_view.h"
#include "cg_local.h"

// Drawing for CTF games
void CTF_DrawStuff(void)
{
	return;
/*
	refEntity_t ent;
	memset (&ent, 0, sizeof(ent));

	VectorCopy(cg.playerHeadPos, ent.origin);

	// Setup the new tiki
	qhandle_t hModel = cgi.R_RegisterModel( "models/ctf_flag_red.tik" );
	int new_tiki = cgi.TIKI_GetHandle( hModel );

	// Process new entity
	//CG_ProcessInitCommands(new_tiki);

	// Fill in the ent fields
	ent.hModel              = hModel;
	ent.scale               = 12;

	cgi.R_AddRefEntityToScene(&ent);*/
}